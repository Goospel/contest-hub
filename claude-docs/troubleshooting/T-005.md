---
summary: 돌연변이 테스트의 치환 범위가 어긋나면 결과를 못 믿는다 — 적게 걸리면 거짓 생존, 많이 걸리면 거짓 사살이다. 치환 횟수를 항상 센다
---

# T-005 · 치환 범위가 어긋난 돌연변이는 결과를 못 믿는다

- **증상**: `lib/verify.ts` 의 날짜 정규식에 돌연변이 7종을 걸었더니 `[-./년]` → `.` (날짜 구분자를 임의 문자로) 하나만 **생존**했다. 테스트 갭이라 판단해 오탐 케이스 두 건을 추가했는데, 같은 돌연변이를 다시 돌리니 **여전히 생존**했다. 새 테스트가 겨냥한 바로 그 동작인데도 안 죽었다.
- **원인**: 정규식에는 구분자 문자 클래스가 **둘** 있다 — 연↔월 사이의 `[-./년]` 과 월↔일 사이의 `[-./월]`. 치환은 `[-./년]` 만 바꿔서 `[-./월]` 이 그대로 남았고, 남은 쪽이 여전히 "구분자가 있어야 한다"를 강제했다. 그래서 `"2026 8 31"` 같은 입력은 돌연변이 상태에서도 계속 매칭에 실패했다 — **돌연변이가 의도한 만큼 약해지지 않은 것이지, 테스트가 무력했던 게 아니다.**
- **해결**: 두 클래스를 모두 치환하니 곧바로 2건이 실패하며 죽었고, 죽인 것은 새로 추가한 오탐 테스트 두 건이었다.
  ```powershell
  $mutated = $orig.Replace('[-./년]', '.').Replace('[-./월]', '.')
  ```
- **재발방지**: 돌연변이가 생존하면 **테스트 갭을 의심하기 전에 치환이 실제로 의도한 전부에 걸렸는지 본다**. 확인법: ① 치환 전후 문자열이 달라졌는지만 보지 말고 **치환 횟수**를 센다 ② 같은 의미의 토큰이 소스에 여러 번 나오는지 grep 한다. 거짓 생존을 그대로 믿으면 이미 충분한 테스트에 불필요한 케이스를 덧붙이거나, 반대로 "테스트가 원래 약하다"는 잘못된 결론을 내리게 된다.

## 반대 방향 — 과잉 치환이 만든 거짓 사살 (2026-07-30 추가)

`lib/normalize.ts` 에서 "실재 날짜 검증 제거" 돌연변이를 `  if (` → `  if (false && ` 로 넣었더니 **치환이 8회** 걸렸다. 겨냥한 날짜 대조뿐 아니라 제목 검사·URL 검사 등 함수 전체의 분기가 한꺼번에 무력화됐다. 테스트는 당연히 실패했고 `KILLED` 로 찍혔지만, **그 사살은 "날짜 검증이 테스트로 보호된다"를 전혀 증명하지 않는다** — 무엇이 죽였는지 알 수 없기 때문이다.

겨냥한 블록 전체를 히어독으로 정확히 지정해 치환 1회로 만든 뒤 다시 돌리니 4건이 실패하며 진짜로 죽었다.

**그래서 치환 횟수는 양쪽으로 본다**: `0회`면 돌연변이가 안 걸린 것, `1회`가 정상, `2회 이상`이면 과잉 치환을 의심하고 경고를 찍는다. 짧은 토큰(`if (`, `return`, `null`)을 치환 대상으로 삼지 않는다.

```powershell
$hits = ([regex]::Matches($orig, [regex]::Escape($from))).Count
if ($hits -eq 0) { "SKIP" } elseif ($hits -gt 1) { "WARN 과잉 치환 가능" }
```
